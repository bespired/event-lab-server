---
    label: Contacts
    dependancy: profiles

    tablename: accu_contacts

    columns:
        id:          incremental
        handle:      handle
        profile:     handle
        project:     base1

        is_changed:   bit
        is_new:       bit

        role:        name  # attr defined types who can do stuff on-site and backend

        email:       name
        firstname:   name
        lastname:    name
        blacklist:   boolean
        in_queue:    base1

        deleted_at:  timestamp-null

        attr(50):    name
        large(10):   large

