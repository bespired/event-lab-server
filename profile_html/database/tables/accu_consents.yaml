---
    label: Consents
    dependancy: profiles

    tablename: accu_consents

    columns:
        id:          incremental
        handle:      handle
        profile:     handle
        cmne:        mnemonic
        project:     base1

        is_changed:   bit
        is_new:       bit

        state(30):   base1
        extend(30):  data
        predate(30): timestamp-null
        optdate(30): timestamp-null
        outdate(30): timestamp-null

