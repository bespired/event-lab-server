---
    label: Tags
    dependancy: profiles

    tablename: accu_tags

    columns:
        id:         incremental
        handle:     handle
        project:    base1

        profile:    handle

        is_changed:   bit
        is_new:       bit

        repeats:
            amount: 100
            columns:
                tag:   base1
                date:  timestamp-null
