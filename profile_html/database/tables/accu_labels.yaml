---
    label: Tags
    dependancy: profiles

    tablename: accu_labels

    columns:
        id:         incremental
        handle:     handle
        project:    base1

        profile:    handle

        repeats:
            amount: 100
            columns:
                label: base1
                date:  timestamp-null
